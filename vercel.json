{
  "version": 2,
  "builds": [
    {
      "src": "server.ts",
      "use": "@vercel/node",
      "config": {
        "includeFiles": ["dist/**", "src/utils/index.ts"]
      }
    }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "/server.ts" }
  ]
}
